#include "ConfigLoader.h"

using namespace TS_SfM;

Config ConfigLoader::LoadConfig(const std::string str_config_file) {
  Config output;

  return output;
}

Camera ConfigLoader::LoadCamera(const std::string str_camera_file) {
  Camera output;

  return output;
}
